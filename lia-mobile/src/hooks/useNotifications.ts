/**
 * Bootstrap + ciclo de vida de notificaciones locales LÍA.
 * Pedir permiso solo desde Perfil/Configuración (nunca al arrancar a ciegas).
 */
export { useNotificationLifecycle as useNotifications } from './useNotificationLifecycle';
